#!/bin/bash
# Default script for nnw new — run when user invokes "nnw new" with no subcommand.
# Add subcommands as new/new-thing.sh and invoke with "nnw new new-thing".
echo "nnw new (default): no subcommand given. Add scripts under new/ or run e.g. nnw new <script>."
